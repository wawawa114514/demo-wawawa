version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
    commands:
      - echo "Installing Maven dependencies..."
      - cd demo-wawawa
      - mvn clean install -DskipTests

artifacts:
  files:
    - "*.jar"
  base-directory: demo-wawawa/target
